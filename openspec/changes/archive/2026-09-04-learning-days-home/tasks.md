## 1. Каталог и переходы

- [x] 1.1 Добавить отдельное назначение Home и список доступных дней со стабильными ключами, номерами, названиями и описаниями; проверить, что список содержит только 02–04 в порядке номера, а Home не становится карточкой.
- [x] 1.2 Реализовать главный экран «AI Advent» с одноколоночным прокручиваемым списком Material 3 карточек; проверить кликабельность всей карточки, отсутствие отдельного фокуса на декоративной стрелке и перенос длинного текста.
- [x] 1.3 Переключить старт без восстанавливаемого состояния на Home, удалить нижнюю панель и подключить открытие соответствующих уроков; обновлённый навигационный UI-тест должен пройти все три маршрута при недоступном backend без вызова генерации.

## 2. Возврат и сохранение состояния

- [x] 2.1 Добавить общую верхнюю панель дня с номером, русским названием и callback «Назад к дням», подключить системный возврат только внутри уроков и устранить дублирование системных отступов; проверить верхнюю стрелку и системное действие назад для каждого дня, закрытие клавиатуры и стандартный выход с Home.
- [x] 2.2 Добавить сохраняемые области UI по стабильным ключам назначений, включая каталог, оставив текущие ViewModel на уровне Activity; целевой UI-тест должен подтвердить сохранение запроса, настроек, результата, прокрутки и раскрытого блока при посещении другого дня и возврате.
- [x] 2.3 Проверить незавершённый запрос и пересоздание Activity на управляемом fake-источнике данных: уход в каталог не отменяет и не дублирует запрос, возврат показывает его актуальный результат или ошибку, пересоздание сохраняет экран и состояние; также проверить независимость дней и сохранение истории Day 04.

## 3. Подписи и документация

- [x] 3.1 Вынести новые общие строки в Android resources, применить согласованные русские подписи Day 02 и проверить остаточные статические английские подписи Day 03–04; проверка отображения и существующие тесты должны подтвердить неизменность API enum, одинакового prompt в COMPARE и точных подписей из действующих спецификаций.
- [x] 3.2 Актуализировать затронутые существующие UI-тесты под вход через каталог и новые подписи, сохранив проверки экспериментов; убедиться, что тесты больше не требуют нижних вкладок и не считают Day 02 стартовым экраном.
- [x] 3.3 Кратко обновить `android-app/README.md`: каталог, возврат и сохранение данных только в текущей сессии; чтением и diff проверить соответствие реализованному поведению и сохранность существующих инструкций запуска.

## 4. Интеграционная проверка

- [x] 4.1 Выполнить из корня проекта через PowerShell 7 команды `scripts/dev.ps1 unit`, `scripts/dev.ps1 build` и `scripts/dev.ps1 ui` (Gradle testDebugUnitTest, assembleDebug и connectedDebugAndroidTest); зафиксировать успешные результаты либо конкретный блокер, не выполняя реальные вызовы OpenAI ради UI-проверок.
- [x] 4.2 Визуально проверить каталог и заголовки трёх дней на обычном телефоне и узком экране с увеличенным шрифтом: весь текст читается, элементы не перекрыты системными панелями, прокрутка и возврат доступны; проверить diff и `openspec validate learning-days-home --strict`, отметить задачи только по фактическому выполнению.

## Результаты проверки (2026-09-04)

- Сборка debug APK и 23 JVM-теста прошли через штатный scripts/dev.ps1.
- UI-набор: 8 из 9 сценариев прошли в общем прогоне; после удаления дублирующего testTag оставшийся сценарий reasoningExpansionAndCatalogScrollSurviveRoundTrip прошёл отдельным прогоном. Проверены реальные пересоздания MainActivity с fake-репозиториями, возврат обоими способами, клавиатура, завершение запроса вне урока, ошибки, история и сохранение прокрутки/раскрытия.
- Каталог и заголовки Day 02–04 визуально проверены на эмуляторе API 34: 1080x2220 при density 440 и font_scale 1.0; 880x1920 (320 dp) при font_scale 1.5. После сокращения подписи лимита токенов APK пересобран, исправленный блок повторно проверен визуально. Исходные параметры эмулятора восстановлены.
- Новые вызовы OpenAI не выполнялись; работа проверена на fake-данных.
