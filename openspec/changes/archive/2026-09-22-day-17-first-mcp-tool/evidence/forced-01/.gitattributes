# Preserve captured bytes and manifest SHA-256 values across Git checkouts.
# Captured Windows CRLF is intentional; retain the other whitespace checks.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
